import { addDigit, deleteDigit, validateInput, updateSettingsOpen, toggleComparison, toggleOperation, toggleTerm, initTerms, } from './logic';
initTerms();
export { addDigit as add_digit, deleteDigit, validateInput, updateSettingsOpen, toggleComparison, toggleOperation, toggleTerm, };

import {
  addSymbol,
  deleteSymbol,
  handleInput,
  toggleSettingsOpen,
  toggleComparison,
  toggleOperation,
  toggleTerm,
  initTerms,
} from './logic'

export {
  addSymbol,
  deleteSymbol,
  handleInput,
  toggleSettingsOpen,
  toggleComparison,
  toggleOperation,
  toggleTerm,
  initTerms,
}
